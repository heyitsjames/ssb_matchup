defmodule SsbMatchup.MatchupBoards.BoardTest do
  use SsbMatchup.DataCase
  alias SsbMatchup.MatchupBoards.Board

  describe "SsbMatchup.MatchupBoards.Board" do
    test "validates to see if it's a valid board" do
      
    end
  end
end